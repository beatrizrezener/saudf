<Page
    x:Name="pageRoot"
    x:Class="saudfhub.UnidadePage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:saudfhub"
    xmlns:common="using:saudfhub.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Directions" Label="Traçar rota" Click="Click_VerNoMapa"/>
            <AppBarButton Icon="AllApps" Label="Listar Rota" Click="Click_ListarRota"/>
            <AppBarButton Icon="Send" Label="Sugestão" Click="Click_ReportarErro"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" x:Name="contentRegion" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,10"
                        Text="TELEFONE" />
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Grid.Row="1" x:Name="TextBlockTelefone"
                        Text="(99) 9999-9999"/>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,15,0,10"
                        Text="ENDEREÇO" Grid.Row="2"/>
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Grid.Row="3" x:Name="TextBlockEndereco"
                        Text="Quadra 999 Bairo XYZ Lote 99"/>
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Margin="0,15,0,10"
                        Text="CATEGORIA" Grid.Row="4"/>
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Grid.Row="5" x:Name="TextBlockTipo"
                        Text="Hospital"/>
        </Grid>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="TextBlockNome" Text="Nome da Unidade" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
    </Grid>
</Page>
